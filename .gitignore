node_modules
dist
out
.DS_Store
.eslintcache
*.log*
release
.idea
.scratch
tsconfig.*.tsbuildinfo

# Local tooling / scratch (fork-only, not upstream)
.claude/
.codex-*/
package-lock.json

# pnpm store written by the Docker Linux build (see the AppImage note)
.pnpm-store
